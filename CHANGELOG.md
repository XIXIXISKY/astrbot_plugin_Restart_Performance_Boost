# 更新日志

## v1.0.2 — 余额查询能力 & Key 自动识别增强

### 余额/用量查询（全新功能）
- 新增 `余额 当前/所有/平台名` — 查询当前平台、所有已配置平台或指定平台余额
- 新增 `余额 key…` — 批量自定义 Key 查询余额
- 新增 `余额 URL…` / `余额 json…` / `余额 yaml` — 自定义端点查询、粘贴 JSON 连接信息、按 YAML 配置批量查询
- 新增 `余额 账户` / `余额 订阅` — 查询 NEW API 账户信息与订阅（订阅需管理员模式）
- 新增 `用量 当前/所有/平台名` — 查询 token 用量
- 新增 LLM 工具 `query_balance`（需开启 `enable_balance_llm_tool`，支持自然对话查余额）
- 模板化输出：成功/失败模板 + `{{?变量}}` 条件行 + `{{smart_balance}}` 附加信息 + `{{api_key}}`
- 新增 ChatAnywhere 平台查询支持
- YAML 自定义服务查询（`{{data.x}}` 路径 + `{{ {表达式} }}` 计算）
- 平台别名扩展配置 `balance_alias_config`
- `admin_only` / `show_unsupported` 可配置

### Key 自动识别增强
- 新增 Key 特征自动匹配：当 api_base 不匹配任何已知平台时，会尝试根据 Key 本身特征识别平台（如 `sk-` 开头自动走 DeepSeek 官方接口查询），不再卡在未知中转站上
- 查询优先级（四步）：api_base 匹配 → Key 特征兜底 → NEW API 专用 URL → 自动探测中转站

## v1.0.1

- 修复：Logo 图片格式改为真正的 PNG（之前为 JPEG 伪装 PNG，导致插件市场无法正常显示）
- 修复：通知目标 QQ 号改为可配置项，不再硬编码
- 优化：README 增加作者与 QQ 徽章，完善安装说明

## v1.0.0

- 初始版本发布。
